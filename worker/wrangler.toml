name = "halyard-counter"
main = "worker.js"
compatibility_date = "2026-09-25"

# The counts live in one KV namespace. Create it once (see README) and paste the id here.
[[kv_namespaces]]
binding = "STATS"
id = "426fe74d9e9f482ab4b338029f76e7db"

# STATS_TOKEN is set as a secret, never written in this file:
#   npx wrangler secret put STATS_TOKEN
